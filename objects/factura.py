class Factura:

    def __init__(self, subtotal, total, forma_pago, valor_pagado, cod_compra):
        self.subtotal = subtotal
        self.total = total
        self.forma_pago = forma_pago
        self.valor_pagado = valor_pagado
        self.cod_compra = cod_compra